<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Plataforma de Cursos | Inicio</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Estilos personalizados adicionales si fueran necesarios */
        body {
            background-color: #f8f9fa;
        }
    </style>
</head>

<body>

    <!-- Navbar Genérico -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-5">
        <div class="container">
            <a class="navbar-brand" href="/">Test VercelPostgres</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarContent">
                <ul class="navbar-nav ms-auto align-items-center">
                    <% if (user) { %>
                        <li class="nav-item me-3">
                            <span class="text-white">Hola, <strong>
                                    <%= user.nombre %>
                                </strong></span>
                        </li>
                        <% if (user.es_premium) { %>
                            <li class="nav-item me-2">
                                <span class="badge bg-success">Premium Activo</span>
                            </li>
                            <% } else { %>
                                <li class="nav-item me-2">
                                    <span class="badge bg-secondary">Cuenta Inactiva</span>
                                </li>
                                <% } %>
                                    <li class="nav-item">
                                        <a href="/logout" class="btn btn-outline-danger btn-sm">Cerrar Sesión</a>
                                    </li>
                                    <% } else { %>
                                        <li class="nav-item">
                                            <a href="/login" class="btn btn-outline-light">Iniciar Sesión</a>
                                        </li>
                                        <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Contenido Principal -->
    <div class="container">
        <div class="row g-4">

            <!-- Tarjeta 1: Contenido Abierto (Verde) -->
            <!-- Esta tarjeta siempre se muestra activa/verde -->
            <div class="col-md-6">
                <div class="card border-success h-100 shadow-sm">
                    <div class="card-header bg-success text-white fw-bold">
                        Módulo Gratuito
                    </div>
                    <div class="card-body text-success">
                        <h5 class="card-title">Introducción al Sistema</h5>
                        <p class="card-text">
                            Este contenido está disponible para todos los usuarios. Aquí puedes acceder a la
                            documentación básica y tutoriales de inicio.
                        </p>
                        <a href="/gratuito" class="btn btn-success">Acceder al contenido</a>
                    </div>
                </div>
            </div>

            <!-- Tarjeta 2: Contenido Restringido -->
            <div class="col-md-6">
                <% if (user && user.es_premium) { %>
                    <!-- ESTADO ACTIVO (Verde) -->
                    <div class="card border-success h-100 shadow-sm">
                        <div class="card-header bg-success text-white fw-bold">
                            Módulo Premium
                        </div>
                        <div class="card-body text-success">
                            <h5 class="card-title">Configuración Avanzada</h5>
                            <p class="card-text">
                                ¡Felicidades! Tienes acceso completo al contenido premium.
                            </p>
                            <a href="/premium" class="btn btn-success">Acceder al contenido Premium</a>
                        </div>
                    </div>
                    <% } else { %>
                        <!-- ESTADO BLOQUEADO (Gris) -->
                        <div class="card border-secondary h-100 bg-light text-secondary shadow-sm">
                            <div class="card-header bg-secondary text-white fw-bold">
                                Módulo Premium
                            </div>
                            <div class="card-body">
                                <h5 class="card-title">Configuración Avanzada</h5>
                                <p class="card-text">
                                    Este contenido es exclusivo.
                                    <% if (user) { %>
                                        Tu cuenta aún no tiene permisos activos. Contacta al administrador.
                                        <% } else { %>
                                            Necesitas iniciar sesión y tener permisos especiales.
                                            <% } %>
                                                <br>
                                                <small><em>Estado actual: Bloqueado</em></small>
                                </p>
                                <button class="btn btn-secondary" disabled>
                                    🔒 Contenido Bloqueado
                                </button>
                            </div>
                        </div>
                        <% } %>
            </div>

        </div>
    </div>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>